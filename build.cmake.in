// build.gen.h
#ifndef DEFINED_BUILD
#define DEFINED_BUILD

#define ${PROJ_PREFIX}_BUILD_RELEASE 0
#define ${PROJ_PREFIX}_BUILD_STAGE 1
#define ${PROJ_PREFIX}_BUILD_DEBUG 2

#define ${PROJ_PREFIX}_VERSION_MAJOR ${PROJ_VERSION_MAJOR}
#define ${PROJ_PREFIX}_VERSION_MINOR ${PROJ_VERSION_MINOR}
#define ${PROJ_PREFIX}_VERSION_PATCH ${PROJ_VERSION_PATCH}
#define ${PROJ_PREFIX}_VERSION "${PROJ_VERSION}"

#define ${PROJ_PREFIX}_BUILD ${PROJ_BUILD}

#if ${PROJ_PREFIX}_BUILD == ${PROJ_PREFIX}_BUILD_DEBUG
#define GAME_DEBUG
#endif

#endif